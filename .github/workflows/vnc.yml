name: VNC with FRPC Tunnel

on: [push]

jobs:
  vnc-frpc:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install Desktop and VNC
        run: |
          sudo apt update
          sudo apt install -y xfce4 xfce4-goodies tigervnc-standalone-server

      - name: Set VNC password
        run: |
          mkdir -p ~/.vnc
          echo "password" | vncpasswd -f > ~/.vnc/passwd
          chmod 600 ~/.vnc/passwd

      - name: Start VNC server
        run: |
          vncserver :1 -geometry 1280x800 -depth 24

      - name: Download and install FRPC
        run: |
          wget https://github.com/fatedier/frp/releases/download/v0.58.0/frpc_0.58.0_linux_amd64.tar.gz
          tar -xzf frpc_0.58.0_linux_amd64.tar.gz
          sudo mv frpc_0.58.0_linux_amd64/frpc /usr/local/bin/
          chmod +x /usr/local/bin/frpc

      - name: Create frpc.ini
        run: |
          cat > frpc.ini <<EOF
          [common]
          server_addr = ${{ secrets.IP }}
          server_port = 1234

          [vnc]
          type = tcp
          local_ip = 127.0.0.1
          local_port = 5901
          remote_port = 15900
          EOF

      - name: Show frpc.ini (optional debug)
        run: cat frpc.ini

      - name: Start frpc and block forever
        run: |
          echo "VNC available at ${{ secrets.IP }}:15900 (use a VNC client)"
          frpc -c frpc.ini
