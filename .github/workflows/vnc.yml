name: Enable Apple VNC without password and FRP tunnel

on:
  push:
    branches:
      - main

jobs:
  vnc-tunnel:
    runs-on: macos-latest

    steps:
      - name: Install frpc
        run: brew install frpc

      - name: Enable Apple Remote Management with legacy VNC but no password, all users allowed
        run: |
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
            -activate -configure -access -on -privs -all -users "" \
            -clientopts -setvnclegacy -vnclegacy yes \
            -restart -agent

      - name: Create frpc.ini
        run: |
          cat > frpc.ini <<EOF
          [common]
          server_addr = ${{ secrets.IP }}
          server_port = 1234

          [vnc]
          type = tcp
          local_ip = 127.0.0.1
          local_port = 5900
          remote_port = 5900
          EOF

      - name: Show frpc.ini
        run: cat frpc.ini

      - name: Run frpc client
        run: frpc -c frpc.ini
